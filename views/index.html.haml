
%h1 all images
@images.each do |image|
%li
  %h2=image.title
   %img{src:imag.file ,width:"100%"}
%h1 Upload new image
%form(action="/imags" method="post" enctype="multipart/form-data")
%p 
%input(type="file" name="image[file]" id="file")
%p 
%input(type="text" name="image[title]" class="form-control" placeholder="Enter a title for the iimage")
%p 
%button(class="btn btn-primary") Upload!
